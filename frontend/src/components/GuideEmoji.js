import React from 'react'
import { guideImg } from '../data/images'

const GuideEmoji = () => {

  return (
    <img src={guideImg} alt="" className={`emoji-character`}/>
  )
}

export default GuideEmoji